{
  "name": "@riacore/desktop-host",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc && electron dist/main.js",
    "test": "vitest --run",
    "prepare:package": "node scripts/prepare-package.cjs",
    "package:dir": "pnpm run prepare:package && electron-builder --config electron-builder.yml --dir",
    "package:win": "pnpm run prepare:package && electron-builder --config electron-builder.yml --win --x64 && node scripts/validate-package.cjs --platform win",
    "package:mac": "pnpm run prepare:package && electron-builder --config electron-builder.yml --mac --arm64 && node scripts/validate-package.cjs --platform mac",
    "package:linux": "pnpm run prepare:package && electron-builder --config electron-builder.yml --linux --x64 && node scripts/validate-package.cjs --platform linux"
  },
  "dependencies": {
    "@riacore/app-contracts": "workspace:*",
    "@riacore/app-core": "workspace:*",
    "@riacore/git-service": "workspace:*",
    "@riacore/importer-sdk": "workspace:*",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@vitest/ui": "^4.1.10",
    "electron": "^43.4.0",
    "electron-builder": "^26.15.3",
    "fast-check": "^4.9.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
